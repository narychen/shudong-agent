# Rust Core

This directory will hold the Rust engine for the agent.

Current status:
- Placeholder crate created manually
- Local Rust toolchain is not installed in this workspace yet
- `cargo` and `rustup` are unavailable on this machine
